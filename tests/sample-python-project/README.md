# Sample Python Project

This is a minimal test project used to verify the uv-ci.yml reusable workflow functions correctly.

It includes:
- A simple Python package with type hints
- Tests using pytest
- Coverage configuration
- Type checking with mypy
- Optional dependencies to test extras handling
